[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "openalgo-utils"
requires-python = ">=3.12"
version = "0.1.0"
description = "Rust-accelerated utilities for OpenAlgo (Indian number format, MPP slab)"

[tool.maturin]
features = ["pyo3/extension-module"]
