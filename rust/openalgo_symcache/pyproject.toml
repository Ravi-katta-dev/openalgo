[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "openalgo-symcache"
requires-python = ">=3.12"
version = "0.1.0"
description = "In-memory O(1) symbol token cache for OpenAlgo"

[tool.maturin]
features = ["pyo3/extension-module"]
